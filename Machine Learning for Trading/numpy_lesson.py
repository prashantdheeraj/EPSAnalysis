""""creating numpy array"""

import numpy as np

# def test_run():
#     #   List of 1D Array
#     # np.array([2,3,4])
#
#     # List of 2D Array
#     # np.array([(2,3,4),(5,6,7)])
#
#     #Create an empty 1d array with 5 column a
#     # np.empty(5)
#
#     # Create an empty 2d array with 5 row and 4 column a
#     # np.empty((5,4))
#
#     # Create an empty 3d array with 5 row ,  4 column and 3 depth
#     # np.empty((5,4,3))
#
#     # the empty Arrays will hold whaever value is at that memory location.The default data type is float
#
#     #So we need to create an array with all values as 1 and make default as integer
#     # np.ones((5,4), dtype = np.int)
#
#
#     # np.random generates an array full of random numbers uniformaly between [0.0 , 1.0] with 0 inclusve and 1 exclusive with 5 rows and 4 column
#     # np.random.random((5,4))
#     # np.random.rand((5,4) # no need to provide a tupe
#
#     # In order to selct a normal distribution use normal
#     # np.random.normal(size=(2,3)) # "standard normal" (mean = 0, s.d =1)
#     # np.random.normal(50,10,size=(2, 3))  # "standard normal" (mean = 50, s.d =10)
#
#     # Random integer
#     # np.random.randint(10) # a single integer between 0 and 10
#     # np.random.randint(0,10, size=5) # 5 random int as a 1 d array
#     # np.random.randint(0, 10, size=(2,3)  # 2X3 array of random
#
#     # a.shape gives the shape of array
#     # a.shape[0] # number of rows
#     # a.shape[1] # number of colums
#     # a.size gives the number of elements in the array
#     # a.dtype gives the data type of array
#
#     # np.random.seed(number) to get the same random number all the time for testing purpose
#     # a.sum() gives sum of all elements of the array
#     # a.sum(axis=0) # axis =0 is sum across all row; axis = 1 is sum across all columsn
#
#     # a.min(axis=0) gets the minimum of each column
#     # a.max(axis=1) gets maximum of each row
#     # a.mean() gets the mean of all elements
#
#     # a.argmax() returns the max value of element and its index
#
#     # indexing an array with another array
#     # a = np.random.rand(5)
#     #accessing using list of indices
#     # indices = np.array([1,1,2,3])
#     # print(a[indices]) # will print the value of a from position 1,1 , 2 and 3
#
#
#     # Boolean or mask index array
#     # a = np.array([(20.25,10,23,26,32,10,5,0),(0,2,50,20,0,1,28,5,0)])
#     # mean = a.mean()
#     # print(a[a<mean])# print all values less than mean
#     # a[a<mean] = mean # Mask the less than mean value with mean

if __name__ == "__main__":
    #test_run()
